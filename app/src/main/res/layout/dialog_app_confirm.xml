<?xml version="1.0" encoding="utf-8"?>
<!--
    Diálogo genérico con el estilo de marca de la app: card oscura con
    borde cian, título con degradado cian->morado (igual al del título
    del reproductor), botón principal con el gradiente de marca y botón
    secundario en outline gris. Se usa para "¿Salir de la app?" y
    "Continuar viendo", y queda listo para reutilizar en cualquier otro
    diálogo de confirmación futuro: solo hay que inflar este layout desde
    AppDialogs.showConfirm(...) y pasarle los textos.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="290dp"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    android:background="@drawable/bg_dialog_app"
    android:padding="22dp">

    <TextView
        android:id="@+id/dialog_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="28sp"
        android:layout_marginBottom="6dp" />

    <TextView
        android:id="@+id/dialog_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/accent"
        android:textSize="17sp"
        android:textStyle="bold"
        android:layout_marginBottom="6dp" />

    <TextView
        android:id="@+id/dialog_message"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/text_secondary"
        android:textSize="13sp"
        android:gravity="center"
        android:lineSpacingExtra="2dp"
        android:layout_marginBottom="18dp" />

    <TextView
        android:id="@+id/dialog_btn_primary"
        android:layout_width="match_parent"
        android:layout_height="46dp"
        android:background="@drawable/bg_chip_active"
        android:gravity="center"
        android:textColor="@color/text_primary"
        android:textSize="14sp"
        android:textStyle="bold"
        android:textAllCaps="false"
        android:layout_marginBottom="10dp"
        android:clickable="true"
        android:focusable="true"
        android:stateListAnimator="@animator/focus_scale_button" />

    <TextView
        android:id="@+id/dialog_btn_secondary"
        android:layout_width="match_parent"
        android:layout_height="44dp"
        android:background="@drawable/bg_pill_outline_muted"
        android:gravity="center"
        android:textColor="@color/text_secondary"
        android:textSize="13sp"
        android:textAllCaps="false"
        android:clickable="true"
        android:focusable="true"
        android:stateListAnimator="@animator/focus_scale_button" />

</LinearLayout>

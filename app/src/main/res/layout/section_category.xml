<?xml version="1.0" encoding="utf-8"?>
<!--
    Opción B (elegida por JOX3): cada categoría vive dentro de su propio
    panel (bg_section_panel) y su título lleva un punto ("section_bullet")
    del mismo color que ya se usa para el texto — así cada sección se
    distingue como un bloque propio en vez de solo texto de color flotando
    sobre la fila.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:layout_marginTop="22dp"
    android:layout_marginStart="12dp"
    android:layout_marginEnd="12dp"
    android:background="@drawable/bg_section_panel"
    android:paddingTop="14dp"
    android:paddingBottom="14dp">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginStart="10dp"
        android:layout_marginBottom="10dp">

        <View
            android:id="@+id/section_bullet"
            android:layout_width="8dp"
            android:layout_height="8dp"
            android:layout_marginStart="4dp"
            android:layout_marginEnd="8dp"
            android:background="@drawable/dot_white_solid" />

        <TextView
            android:id="@+id/section_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/text_primary"
            android:textSize="15.5sp"
            android:textStyle="bold"
            android:drawablePadding="6dp"
            android:gravity="center_vertical"
            android:clickable="true"
            android:focusable="true"
            android:stateListAnimator="@animator/focus_scale_row"
            android:padding="4dp" />
    </LinearLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/section_recycler"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingStart="14dp"
        android:paddingEnd="14dp"
        android:clipChildren="false"
        android:clipToPadding="false" />

</LinearLayout>
